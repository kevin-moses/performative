[package]
name = "performative-osc"
version.workspace = true
edition.workspace = true

[dependencies]
rosc = "0.10"
tokio = { version = "1", features = ["net", "sync", "time", "rt"] }
anyhow = "1"
tracing = "0.1"
